
async function loadReseller(){ const res=await fetch('/data/products_combined_A_D.json'); const prods=await res.json(); const grid=document.getElementById('r_products'); prods.filter(p=>p.category==='pulsa' || p.category==='game').slice(0,12).forEach(p=>{const c=document.createElement('div');c.className='card'; const name=p.item||p.nominal||p.name; c.innerHTML=`<h3>${name}</h3><p>Rp ${p.prices.A.reseller}</p><button onclick="buy('${p.id}')">Beli</button>`; grid.appendChild(c) }); document.getElementById('saldo').innerText='Rp 150000 (demo)'; } function buy(id){ alert('Demo beli: '+id) } document.addEventListener('DOMContentLoaded', loadReseller);
