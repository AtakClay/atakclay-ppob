
async function loadProducts(){ const res=await fetch('/data/products_combined_A_D.json'); const prods=await res.json(); const cat=document.getElementById('categories'); const grid=document.getElementById('products'); const cats=['pulsa','data','pln_token','ewallet','game','billing']; cats.forEach(c=>{const d=document.createElement('div');d.className='card';d.innerHTML=`<h3>${c.toUpperCase()}</h3>`;cat.appendChild(d)}); prods.slice(0,12).forEach(p=>{const c=document.createElement('div');c.className='card'; const icon = p.category==='game'? '/public/icons/game/'+p.game.toLowerCase().replace(/ /g,'_')+'.png' : '/public/icons/ppob/'+(p.provider||'telkomsel').toLowerCase().replace(/ /g,'_')+'.png'; c.innerHTML=`<img src="${icon}" style="width:80px;height:80px"><h3>${p.item||p.nominal||p.name}</h3><p>Rp ${p.prices.A.customer}</p><button onclick="alert('Demo beli: '+(p.item||p.nominal))">Beli</button>`; grid.appendChild(c) }); } document.addEventListener('DOMContentLoaded', loadProducts);
